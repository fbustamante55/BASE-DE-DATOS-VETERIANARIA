SELECT object_name, object_type, status
FROM user_objects
WHERE object_type = 'PROCEDURE'
ORDER BY status, object_name;


SET SERVEROUTPUT ON;
BEGIN
FIDE_SERVICIOS_MAS_USADOS_X_ESPECIE_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_CLIENTES_CON_MAS_DE_UNA_MASCOTA_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_MASCOTAS_X_CLIENTE_SP(1);
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_NOMBRES_UNICOS_MASCOTAS_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_CITAS_CANCELADAS_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_CITAS_PENDIENTES_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_PRODUCTOS_NO_ACTIVOS_SP;
END;

SET SERVEROUTPUT ON;
BEGIN
FIDE_CAMBIAR_ESTADO_MASCOTA_SP(1, 2); 
END;

commit;



